import { LockKeyhole, Mail } from 'lucide-react'
import { useState, type FormEvent } from 'react'
import { useAuth } from '../contexts/AuthContext'
export function LoginPage(){const{signIn}=useAuth();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState<string|null>(null);const[busy,setBusy]=useState(false);async function handleSubmit(e:FormEvent){e.preventDefault();setBusy(true);setError(await signIn(email,password));setBusy(false)}return <main className="login-page"><div className="login-visual"><div><span className="eyebrow light">Gestão inteligente</span><h2>Decisões melhores começam com números organizados.</h2><p>Custos, receitas, vendas e precificação em um único lugar.</p></div></div><form className="login-card" onSubmit={handleSubmit}><img src="/logo-cia-do-caldinho.png" alt="Cia. do Caldinho"/><div><h1>Bem-vinda</h1><p>Acesse a gestão financeira do restaurante.</p></div><label>E-mail<div className="input-with-icon"><Mail size={17}/><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="voce@email.com"/></div></label><label>Senha<div className="input-with-icon"><LockKeyhole size={17}/><input type="password" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></div></label>{error&&<div className="form-error">{error}</div>}<button className="btn btn-primary btn-full" disabled={busy}>{busy?'Entrando…':'Entrar'}</button><small className="login-help">Acesso restrito aos usuários cadastrados.</small></form></main>}
